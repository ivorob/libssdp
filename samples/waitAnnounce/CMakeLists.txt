CMAKE_MINIMUM_REQUIRED (VERSION 3.0)
PROJECT (waitAnnounce)

SET (SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET (SOURCES
        ${SRC_DIR}/main.cpp)

ADD_EXECUTABLE (waitAnnounce ${SOURCES})
TARGET_LINK_LIBRARIES (waitAnnounce ssdp)
