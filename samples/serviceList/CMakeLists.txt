CMAKE_MINIMUM_REQUIRED (VERSION 3.0)
PROJECT (serviceList)

SET (SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET (SOURCES
        ${SRC_DIR}/main.cpp)

ADD_EXECUTABLE (serviceList ${SOURCES})
TARGET_LINK_LIBRARIES (serviceList ssdp)
