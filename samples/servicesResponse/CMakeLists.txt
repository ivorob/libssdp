CMAKE_MINIMUM_REQUIRED (VERSION 3.0)
PROJECT (servicesResponse)

SET (SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET (SOURCES
        ${SRC_DIR}/main.cpp)

ADD_EXECUTABLE (servicesResponse ${SOURCES})
TARGET_LINK_LIBRARIES (servicesResponse ssdp)
